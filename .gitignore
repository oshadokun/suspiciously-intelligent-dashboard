# Claude Code local session files
.claude/

# Server runtime artifacts
server.log
server_error.log
.server_pid

# Logs
*.log

# Environment / secrets
.env
.env.*
*.env

# OS artifacts
.DS_Store
Thumbs.db
desktop.ini

# Windows temp / cache
Temp/
temp/
tmp/
cache/
Cache/
*.tmp
*.temp

# Node (future-proofing)
node_modules/

# PowerShell session artifacts
*_history.xml
ConsoleHost_history.txt

# Browser local data
LocalStorage/
IndexedDB/

# Daily scan archives (generated data, grows with every run)
scans/20*/

# Failure diagnostics (runtime data)
scans_failed/20*/

# Failed scan manifest (runtime log)
logs/failed_scan_manifest.json

# Build / transfer artifacts
*.zip
